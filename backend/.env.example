# MongoDB connection (local MongoDB or MongoDB Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB_NAME=batris

# Long random secret used to sign the HTTP-only session cookie.
BATRIS_AUTH_SECRET=replace-with-a-long-random-secret

# Set to 1 in HTTPS production.
BATRIS_COOKIE_SECURE=0
